# Orchestrator schedules implementation Tickets only

Files under `.scratch/*/issues/` with a wayfinder `Type:` (`research` / `prototype` / `grilling` / `task`) are not Tickets for the Orchestrator. A missing `Type:` means implementation.
