# CLI name is orchestrator

The binary is `orchestrator`. Usage: `orchestrator <target>` with `--model`, `--thinking-level`, `--concurrency` overriding Target `.scratch/orchestrator.yaml`. `--detach` and `inspect` are ADR-0021. Parsing is `cac` (ADR-0022).
