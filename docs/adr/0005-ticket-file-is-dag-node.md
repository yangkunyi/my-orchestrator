# DAG node is a Ticket file

The scheduled unit is one implementation issue file, not a Feature directory. One Ticket = one branch + one Worktree + one Implementation Agent run.

Rejected: one execution per `.scratch/<slug>/` folder.
