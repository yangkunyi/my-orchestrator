# DAG node is a Ticket file

The scheduled unit is one implementation issue file, not a Feature directory. One Ticket = one branch + one Worktree.

Rejected: one execution per `.scratch/<slug>/` folder.

The "one Implementation Agent run" sentence is superseded by ADR-0030: Retry is another session on the same Worktree.
